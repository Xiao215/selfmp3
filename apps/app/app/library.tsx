/**
 * The library route: every song, with its tag strip and its own filter.
 *
 * A route file renders a feature and does nothing else. The screen and
 * everything it knows live in `src/features/library/`.
 */
export { LibraryScreen as default } from '../src/features/library/LibraryScreen'
