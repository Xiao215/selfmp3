{
  "name": "@selfmp3/doorman",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "self.mp3 doorman — a Cloudflare Worker that signs you in with Google and passes your devices' reads and writes through to your bucket.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "@selfmp3/shared": "*",
    "aws4fetch": "^1.0.20",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "wrangler": "^4.131.1"
  }
}
