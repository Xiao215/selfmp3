/**
 * Keep Unistyles' CSS rules for the life of the page: a browser's concern
 * (`keepWebStyles.web.ts`). A phone's styles are not CSS, so nothing here.
 */
export function keepWebStyles(): void {}
