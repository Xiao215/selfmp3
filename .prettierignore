node_modules
dist
library
data
*.md
package-lock.json
.claude/
.wrangler/

# Built by `npm run build:sw`, and minified. Ignored by apps/app/.gitignore,
# which Prettier does not read: it consults the root .gitignore only.
apps/app/public/sw.js

# Native projects `expo run:*` generates (git-ignored in apps/app/.gitignore, which prettier does not read)
apps/app/ios
apps/app/android

# The agreed UI mock, exported from the design canvas as plain HTML (docs/ui-mock/README.md)
docs/ui-mock
