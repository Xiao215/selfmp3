/**
 * The note mark the app wears beside its name (docs/ui-mock `E1`–`E5`), as one
 * SVG path in a 24 box. Its own file with nothing imported, because the content
 * script draws it in the pill and must stay small.
 */
export const NOTE_PATH = 'M9 4.2 21 2v13.3a3.2 3.2 0 1 1-2-3V6.4l-8 1.5v9.4a3.2 3.2 0 1 1-2-3z'
