/**
 * Whether a full-screen modal is drawn over the whole app, its tab bar and mini
 * player included. On a phone it is a native modal, above everything the shell
 * draws; in a browser it is a page inside the shell's content area.
 */
export const modalCoversScreen = true
