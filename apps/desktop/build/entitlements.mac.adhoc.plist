<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    The unsigned tier: an ad-hoc signature (`identity: "-"`), which is what a
    build with no certificate gets.

    Everything in entitlements.mac.plist, plus the two V8 needs when the code
    signature is not one macOS will validate pages against. Without them an
    ad-hoc build crashes on launch rather than merely warning, which is a much
    worse first impression than "unidentified developer".
  -->
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
