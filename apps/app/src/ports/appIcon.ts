/**
 * The app's icon in this device's accent. On a phone the home-screen icon is
 * baked into the build, so there is nothing to change at runtime.
 */
export function setAppIconHue(_hue: number): void {}
