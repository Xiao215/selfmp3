/**
 * The typewriter face the Receipt look is set in.
 *
 * A platform fact rather than a choice: each system names its own, and there
 * is no font to ship for it. The browser's list is `monoFont.web.ts`.
 */
export const monoFont = 'Menlo'
