{
  "extends": "./tsconfig.base.json",
  // Not in the shared base: `include` resolves relative to the file that
  // wrote it, so one there would point all three suites at this folder.
  "include": ["**/*.ts"]
}
